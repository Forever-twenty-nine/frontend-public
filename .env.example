# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# Este archivo sirve como plantilla. Copia este archivo según el entorno:
# - Para desarrollo local: .env.local
# - Para Docker: las variables se pasan vía docker-compose y Dockerfile
#
# IMPORTANTE NEXT.JS:
# - Variables NEXT_PUBLIC_*: Se embeben en el bundle durante BUILD TIME
# - Variables sin NEXT_PUBLIC_: Solo disponibles en server-side (SSR/API)
# ============================================

# ============================================
# DESARROLLO LOCAL (sin Docker)
# ============================================

# Entorno de ejecución
NODE_ENV=development

# URL del backend para llamadas desde el NAVEGADOR (client-side)
# Esta variable se embebe en el código JavaScript durante el build
NEXT_PUBLIC_URL_BACK=http://localhost:8080/api/v1

# URL del backend para llamadas desde el SERVIDOR (SSR)
# Solo disponible en funciones server-side de Next.js
NEXT_PUBLIC_URL_BACK_SSR=http://localhost:8080/api/v1

# Deshabilitar telemetría de Next.js
NEXT_TELEMETRY_DISABLED=1

# ============================================
# DOCKER (estas variables se pasan vía docker-compose y Dockerfile)
# ============================================
# NEXT_PUBLIC_URL_BACK=http://localhost:8080/api/v1
# NEXT_PUBLIC_URL_BACK_SSR=http://backend-public:8080/api/v1
# NEXT_TELEMETRY_DISABLED=1
# NODE_ENV=production
# PORT=80
# HOSTNAME=0.0.0.0
#
# NOTA: En Docker, NEXT_PUBLIC_URL_BACK_SSR usa el nombre del servicio 'backend-public'
# para comunicación interna entre contenedores